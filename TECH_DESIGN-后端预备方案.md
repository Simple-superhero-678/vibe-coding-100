# 后端数据库 · 预备方案

> 2026-09-20 起草 ｜ 版本 v0.1（**草案 · 未启用**）
> 上游：`TECH_DESIGN.md`（方案 A 纯静态，本版已定案）· `PRD.md`（第 7 节：账号体系 = 太早）
> 一句话：**这份文件是备胎。现在不用，将来要用了直接照着做；一直不用就把这份文件删掉。**

---

## 0. 状态声明（先看这段）

| 项 | 状态 |
|---|---|
| 当前是否启用 | ❌ **未启用**。本版仍按纯静态跑：无后端、无数据库、无账号、无密钥 |
| 本文件的作用 | 预留决策与设计，避免将来真要做时从零想一遍 |
| 启用条件 | 见第 1 节，**四条里满足任意一条**才启动 |
| 不启用怎么办 | 直接删掉本文件即可。**删除不影响任何现有代码**——本文件没有一行会被程序读取 |
| 与 `TECH_DESIGN.md` 的关系 | `TECH_DESIGN.md` 管「本版怎么建」，本文件管「将来若要加后端怎么加」。**两边的结论不冲突**：一个说现在不做，一个说将来怎么做 |

---

## 1. 什么条件下才启用

**门槛（满足任意一条才启动，一条都不满足就别做）**

| # | 触发条件 | 怎么判断它真的发生了 |
|---|---|---|
| 1 | **用户抱怨收藏丢了** | 有人明确说「换手机后收藏没了」「清了缓存收藏没了」——不是我设想，是真人反馈 |
| 2 | **本地版已验证成立** | M6（五部全通）之后，且自用一段时间觉得这站确实有用 |
| 3 | **要知道谁在用 / 查了什么** | 需要留存与使用数据来判断内容方向 |
| 4 | **要在网页上直接改数据** | 觉得「改数据还要动 JSON + 推 git」太麻烦，想要一个在线编辑后台 |

**反向判断（出现这些情况说明还不到时候）**

- 站点还没到 M6 → 太早，前置条件不成立
- 「先搭好反正以后要用」→ 这是为假想需求付成本，不做
- 「别人都有后端」→ 不是理由

---

## 2. 要解决的到底是什么需求

**核心（也是唯一真正硬的需求）：收藏跟着人走，跨设备、跨浏览器。**

其余都是衍生的：

| 需求 | 真需要后端吗 |
|---|---|
| 收藏跨设备同步 | ✅ 必须（这是后端存在的唯一硬理由） |
| 内容在线编辑 / 运营后台 | ✅ 需要（但也可以继续用 git 改，只是麻烦） |
| 浏览统计（谁看了什么） | ✅ 需要 |
| 用户能投稿/评论 | ✅ 需要（但对应用户量得先起来） |
| 存条目数据本身 | ❌ 不需要。JSON 放 git 更合适：能 diff、能回滚、不用付费 |

> **关键判断：条目数据永远优先留在 `data/*.json`。** 数据库只存「用户产生的数据」（收藏、账号、统计）。别把古籍条目搬进数据库——那是给自己找运维活。

---

## 3. 三种后端形态对比

| | **B1 · 托管数据库 + 第三方登录**（推荐） | **B2 · 云函数 + 云数据库** | **B3 · 自建服务** |
|---|---|---|---|
| 形态 | 静态前端直连托管数据库（带行级安全的 BaaS，如 Supabase 类产品），登录走 GitHub OAuth | 静态前端 → 云函数（Serverless）→ 云数据库 | 静态前端 → 自己的一台服务器（Node + Postgres） |
| 要写多少服务端代码 | **几乎不用写**，配表 + 开权限 | 要写若干云函数（每个接口一个） | 全都要写，还要管进程、域名、证书、备份 |
| 学习成本 | 中：表设计 + 行级安全规则 + OAuth 配置 | 较高：再加一层函数部署与调试 | 高：运维知识是另一个领域 |
| 费用 | 免费额度通常够个人用；需**新注册账号** | 同类，额度口径不同 | 服务器固定月费 + 域名 + 时间成本 |
| 运维负担 | 低（平台托管） | 中 | 高（宕机、升级、备份都归你） |
| 排错难度 | 中：故障点在「浏览器 + 平台控制台」两处 | 高：三处（浏览器 + 函数日志 + 数据库） | 最高 |
| 对现有代码改动 | **只改 2 个文件**（见第 5 节） | 同左 + 新增一个接口层文件 | 同左 |
| 结论 | ✅ **将来真要加，选这个** | 仅在 B1 额度或功能不够时考虑 | ❌ 不做 |

**为什么推荐 B1**：它把「后端」这件事压缩到接近零代码——数据库和鉴权都由平台提供，我们只负责配权限和调接口。对一个人维护的小站，这是性价比最高的。

> ⚠️ 三个形态的**具体额度数字都没有实测**，不做结论。等真要启用时，第一件事是登录平台控制台核对当前免费额度与限制。

---

## 4. 数据表设计（预备，未创建）

### 4.1 表 1：`users` — 用户

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | uuid / 主键 | 平台生成的用户主键 |
| `provider` | text | 登录来源，如 `github` |
| `provider_uid` | text | 第三方平台给的唯一 id |
| `nickname` | text | 昵称（用于界面显示，可为空） |
| `created_at` | timestamptz | 首次登录时间 |

**不存的东西**：密码、手机号、邮箱、真实姓名。**一概不要。** 见第 7 节。

### 4.2 表 2：`favorites` — 收藏（核心表）

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | uuid / 主键 | 行主键 |
| `user_id` | uuid / 外键 → `users.id` | 谁的收藏 |
| `entry_id` | text | 条目 id（如 `yishou-001`），**与 `data/*.json` 里的 `id` 完全一致** |
| `created_at` | timestamptz | 收藏时间（用于排序） |

**约束**：`(user_id, entry_id)` 建**唯一索引** —— 防同一条被重复收藏。
**索引**：`user_id` 建索引（查询永远按用户查）。

> ⚠️ `entry_id` **不建外键**约束（因为条目不在数据库里）。后果：条目被删掉后，收藏会指向不存在的 id —— 前端必须处理（显示「此条已下架」并可移除），这条在 `TECH_DESIGN.md` §12.4 已经写了。

### 4.3 表 3（可选）：`events` — 浏览/搜索埋点

只在触发条件 3（要知道谁在用）成立时才建。

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | bigserial | 自增主键 |
| `user_id` | uuid / 可空 | 未登录为空 |
| `event_type` | text | `search` / `open` / `filter` / `export` |
| `payload` | jsonb | 搜索词、条目 id、筛选条件等 |
| `created_at` | timestamptz | 时间 |

### 4.4 表 4（可选）：`entries` — 条目在线编辑

**只在触发条件 4（要在线改数据）成立时才建**，且建成后要么 JSON 与数据库二选一为准，要么定死同步方向（推荐：**数据库为准，定期导出回 JSON 并提交 git 备份**）。

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | text / 主键 | `yishou-001` |
| `bu` | text | 部 |
| `data` | jsonb | 除 id 外的全部字段原样存（`名`／`别名`／`原文`／`出处`／`写小说怎么用`／`异说`／…） |
| `updated_at` | timestamptz | 最后修改时间 |
| `updated_by` | uuid → `users.id` | 谁改的 |

> 用 `jsonb` 存整条，是为了**不改表结构就能加字段**（schema v2 时不用写 migration）。代价是不能按字段建索引，但条目量小（<1000），无所谓。

---

## 5. 前端要改哪里（改动被控制住的原因）

分层设计的红利：**视图层不动。**

| 文件 | 改动 |
|---|---|
| `js/store.js` | 收藏从 `localStorage` 换成调数据库接口。**接口签名保持不变**（`list/has/toggle/remove`），所以视图层完全不用改 |
| `js/auth.js`（新增） | 登录/登出/当前用户状态 |
| `js/data.js` | **仅当**条目也上云时才改（触发条件 4）。否则一行不动 |
| `js/views/*` | **零改动** |
| `css/*`、`index.html` | 只加登录按钮与状态区，不动布局逻辑 |

**渐进增强原则**（必须遵守）：

- **不登录也能用全部浏览功能**，收藏退化成本地 `localStorage`；
- 登录后才把本地收藏**合并上传**，之后走云端；
- 绝不出现「登录墙」——PRD 7.2 判「账号体系 = 太早」就是因为登录墙劝退，这条底线别破。

**首次登录的合并逻辑**：本地收藏列表 ∪ 云端收藏列表 → 写回云端，然后清本地标记（`yishou.fav.migrated = v1`）。

---

## 6. 迁移步骤（真启用时按这个顺序走）

1. **确认触发条件成立**（第 1 节），并明确只做哪几条需求；
2. 注册平台账号 → 建项目 → 建表（第 4 节）→ 开**行级安全**（第 7 节）；
3. 配 GitHub OAuth 登录；
4. 写 `js/auth.js` + 改 `js/store.js`（保持函数签名）；
5. **本地先跑通**：不登录 → 浏览正常、收藏落本地；登录 → 收藏上云、换浏览器还在；
6. 更新 `TECH_DESIGN.md`（把「无后端」那几节改成「已加后端」，记录改了什么）；
7. 更新 `PRD.md` 第 7.2 节那条判断（「太早」→「已启用，因为…」）；
8. 上线、观察一周、再看要不要继续扩（统计、UGC……）。

**每一步都要单独验证**，别一气做完。

---

## 7. 安全与合规红线（启用后必须做到）

| # | 红线 | 为什么 |
|---|---|---|
| 1 | **绝不存密码** | 只用第三方登录（GitHub OAuth）。存密码 = 要处理哈希、加盐、找回、泄露通知，全是坑 |
| 2 | **密钥只能放服务端** | 前端 JS 是公开的（F12 就能看），任何写到前端的密钥等于公开。若 B2 形态，密钥放云函数的环境变量 |
| 3 | **必须开行级安全（RLS）** | 数据库默认权限容易过宽：必须做到「用户只能读写自己 `user_id` 的行」。漏了这条 = 任何登录用户能看所有人的收藏 |
| 4 | **不收个人信息** | 不要手机号、不要邮箱、不要真实姓名。用第三方登录拿到的 id 就够 |
| 5 | **不做自研鉴权** | 自己写 token 校验几乎必然有漏洞。用平台的 |
| 6 | **`entry_id` 必须做输入校验** | 防止用户塞进超长/异常字符串 |
| 7 | **免费额度要有监控** | 额度耗尽站点直接挂；要么加告警，要么接受风险并写明 |

---

## 8. 停用 / 回滚（怎么删干净）

| 情形 | 怎么处理 |
|---|---|
| 评估后决定不做 | 删本文件即可，零代码改动 |
| 上线后不想要了 | ① 前端改回 `localStorage` 版 `store.js`（保留旧版备份）② 关掉平台项目 ③ 删账号或保留（保留不影响）④ 更新两份文档 |
| 想保留数据但关掉服务 | 平台控制台导出收藏表为 CSV，归档到知识库，再关服务 |

**回滚成本不高**，因为改动被限制在 `store.js` 与 `auth.js` 两个文件 —— 这也是第 3 节选 B1 的理由之一。

---

## 9. 待定与待核实

| # | 项 | 状态 |
|---|---|---|
| 1 | 具体平台选哪家 | **未定**。真要启用时再对比（当前市场变化快，现在选完可能过期） |
| 2 | 免费额度具体数字 | **未实测**。启用前先登录控制台核对 |
| 3 | 条目数据是否也上云 | 倾向**不上云**，除非确定要在网页上改数据 |
| 4 | 是否做浏览统计 | 未定，取决于是否真需要判断内容方向 |
| 5 | 启用与否 | **由宝哥在后续打卡中明确说**。不说 = 不启用 |

---

v0.1 · 2026-09-20（草案，未启用）

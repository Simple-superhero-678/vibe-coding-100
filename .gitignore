# 环境变量文件（本地私有，绝不入库）
.env
.env.local
.env.*.local

# 依赖与构建产物
node_modules/
dist/
build/

# 日志
*.log
npm-debug.log*

# 编辑器 / 系统生成
.vscode/
.idea/
.DS_Store
Thumbs.db

# WorkBuddy 本地工作数据（含每日工作日志，属工具数据不入库）
# 注意必须写 .workbuddy/* 而不是 .workbuddy/ —— 父目录被整目录排除时，
# !.workbuddy/skills/ 这类例外不会生效（git 的硬规则）。
.workbuddy/*
# 例外：项目级 Skill 属项目约定（Day 12 起），要入库
!.workbuddy/skills/

# 打卡上传中间产物（含 ima COS 临时凭证 JSON，绝不入库）
checkin/_build/
*.genie

# 私有版图片（观山海等版权图源）：永不入库，规则见 TECH_DESIGN.md §4.4.4
# 顺序硬规矩：先有这一行，再往 assets-private/ 里放图
yizhi/assets-private/

# 出图过程样张（预览/样图/）：带水印的原始输出 + 中间裁切版
# 依据 TECH_DESIGN §11.3「预览/ 属过程文件，不进发布内容」；且图片一旦入库就永久留在历史里
yizhi/预览/样图/
